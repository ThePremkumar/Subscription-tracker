server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;  # handles React Router
    }

    location /api/ {
        proxy_pass http://backend:3000/;   # forwards /api calls to backend container
    }
}